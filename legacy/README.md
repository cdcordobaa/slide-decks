# Legacy / archived

Superseded approaches, kept for reference:
- `specs/` — v1 "designer-ready YAML" spec model (before the card model)
- `scripts/build-deck.mjs` — v1 designer-spec renderer
- `scripts/build-cards.mjs` + `theme/notebooklm-cards.css` — the light NotebookLM card deck
- `scripts/build-blueprint.mjs` + `cards/_blueprint/` — the single-slide blueprint proof
- `theme/harness-theme.css` — the original dark blueprint theme
- `cards/_samples/` — early hybrid image samples

The active pipeline is `scripts/build-blueprint-deck.mjs` + `theme/blueprint.css` (Globant) over `cards/`.
